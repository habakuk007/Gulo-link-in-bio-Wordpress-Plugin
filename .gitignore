# ── Dependencies ────────────────────────────────────────────────────
vendor/
node_modules/

# ── Build artifacts ──────────────────────────────────────────────────
*.zip
dist/

# ── WordPress core / test suite ──────────────────────────────────────
/wordpress/
/tmp/

# ── wp-env (Docker-based test environment) ───────────────────────────
.wp-env/

# ── Composer ─────────────────────────────────────────────────────────
composer.lock

# ── npm ──────────────────────────────────────────────────────────────
package-lock.json

# ── IDE & OS ─────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
*.suo
*.user

# ── PHP coverage ─────────────────────────────────────────────────────
.phpunit.result.cache
coverage/
clover.xml

# ── Secrets / environment ────────────────────────────────────────────
.env
.env.local
*.env

# ── VS Code workspace state (keep extensions.json and settings.json) ─
.vscode/*.code-workspace
.claude/settings.local.json
